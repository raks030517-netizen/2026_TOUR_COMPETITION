server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:file:./data/roamate-local;AUTO_SERVER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
